# Work profile shell configuration.
# Add work-specific aliases, functions, and tool setup here.

# ── Git identity reminder ─────────────────────────────────────────────────────
# Quick check that you're committing with the right identity.
alias git-whoami='echo "$(git config user.name) <$(git config user.email)>"'

# ── Placeholders — fill these in as needed ────────────────────────────────────
# alias vpn-up='...'
# alias vpn-down='...'
# export AWS_PROFILE="..."
# export KUBECONFIG="$HOME/.kube/work-config"
