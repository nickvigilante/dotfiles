# Ubuntu / Debian packages
# Installed via: grep -Ev '^#|^$' packages.apt | xargs sudo apt install -y

# Build essentials
build-essential
curl
wget
git
unzip
zip
gnupg
lsb-release

# Shell
zsh
tmux
tree

# System tools
htop
net-tools
software-properties-common
apt-transport-https
ca-certificates

# Clipboard support
xclip
wl-clipboard
